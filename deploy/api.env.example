# =============================================================================
# 换机只需填这一项。地址 / 模型 / 开关已内置，不要改。
# 复制：copy deploy\api.env.example python\data\api.env
# 真实密钥文件已 gitignore，勿提交。
# =============================================================================

# 阿里云 DashScope（千问）API Key，语音语义 + 人脸视觉共用
LLM_API_KEY=
